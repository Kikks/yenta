# Agent framework
langgraph>=0.2.50

# LLM
anthropic>=0.39.0

# Observability (v3 API — OTel-based, `observe` top-level, `usage_details`)
langfuse>=3.0.0,<4.0.0

# GitHub
PyGithub>=2.4.0

# Diff parsing
unidiff>=0.7.5

# Models + config
pydantic>=2.8.0
python-dotenv>=1.0.1

# HTTP (used by github_client fallbacks)
httpx>=0.27.0

# Tests
pytest>=8.0.0
